# Create FinnGen BigQuery Connection

Create FinnGen BigQuery Connection

## Usage

``` r
fg_connection(environment)
```

## Arguments

- environment:

  Environment identifier (e.g., "build", "review", or "sandbox-XX")

## Value

A BigQuery connection object
