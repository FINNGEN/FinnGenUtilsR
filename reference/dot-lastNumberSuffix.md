# Get Last Number Suffix

Get Last Number Suffix

## Usage

``` r
.lastNumberSuffix(stringVector, prefix)
```

## Arguments

- stringVector:

  Vector of strings with number suffixes

- prefix:

  Prefix before the number (either "r" or "v")

## Value

Character string with the highest number suffix
