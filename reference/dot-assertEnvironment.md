# Assert Environment

Assert Environment

## Usage

``` r
.assertEnvironment(environment)
```

## Arguments

- environment:

  Environment identifier to validate

## Value

NULL (called for side effects)
